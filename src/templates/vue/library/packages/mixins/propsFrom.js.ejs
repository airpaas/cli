export default {
  methods: {
    validate(elFormRefName = 'form') {
      const formRef = this.$refs[elFormRefName]
      if (formRef) {
        return formRef.validate()
      }
      return Promise.resolve()
    },
  },
}
