import { AirPaaSPropsFormProps, AirPaaSTableFormatterProps } from './../types';

import { AirPaaSProps } from '../types';

export type <%= name %>Value = string;

export type <%= name %>Props = Partial<AirPaaSProps<<%= name %>Value>> & {
  // 在这里定义组件的props
};

export type <%= name %>PropsFormProps = AirPaaSPropsFormProps<
  Exclude<<%= name %>Props, 'value' | 'onChange' | 'readonly' | 'disabled'>
>;

export type <%= name %>TableFormatterProps = AirPaaSTableFormatterProps<<%= name %>Value, <%= name %>Props>;
